# ROADMAP — 戦史地図

「戦史地図」の今後の方針をまとめたもの。現状の到達点、短中期の機能拡張、そして長期のビジョン（参戦国全体への拡大・多言語対応・動的なデザイン刷新）を段階的に整理する。

> 本プロジェクトの主題は **「戦域（前線）」と「動員地域（郷土）」を結びつけて見せること**。すべての拡張はこの主題を強化する方向で検討する。

---

## 現状（実装済み）

- 太平洋戦争の戦い **12件**（ペリリュー / 硫黄島 / ガダルカナル / ミッドウェー / サイパン / インパール / 真珠湾 / マレー攻略 / アッツ島 / タラワ / レイテ沖 / 沖縄戦）。
- 2枚の地図の連動：**戦域図**（太平洋中心の世界地図）と **動員地域**（日本地図）。
- 戦域図の **ホイール拡大・ドラッグスクロール・ズームボタン**（拡大時もマーカーは定寸維持）。
- 戦い選択時、日本地図が **動員地域へ自動で寄って拡大**（アニメーション）。
- 戦域図の **陣営の色分け**（日本 / 友好国＝枢軸 / 敵対国＝連合国 / 中立）。
- **歴史的境界線**（1942 影響圏＝点線塗り、1943 絶対国防圏＝破線）。凡例から表示/非表示を切替可。
- 配色レビューを反映（赤の分離・連合国の縹色・境界線を金から分離）。
- 単一 `index.html` 完結。依存ライブラリなしの素の JS/SVG。

### 既知の制約・弱み
- **時間軸が無い**（戦いが並列で、開戦→終戦の流れが読めない）。
- **探索の入口が地図クリックのみ**（一覧・検索・絞り込みが無い）。
- **共有性が無い**（URL に状態が乗らない、OGP 等のメタタグ未整備）。
- データが HTML 埋め込みで、追加・裏取り・校正の運用が重い。
- データはプロトタイプ用の **仮データ**で、一次資料による裏取りが未了。

---

## フェーズ別ロードマップ

### Phase 0 — 基盤整備（最優先・全拡張の土台）

| 項目 | コスト | 内容 |
|------|--------|------|
| **データの外部化** | 小 | `BATTLES` を別ファイルへ切り出す。`battles.js`（`const BATTLES=[...]` を `<script>` 読込）にすれば `file://` 直開きでも動く。将来 JSON 化する場合は CORS のためサーバー前提になる点に注意。 |
| **データ構造の拡張** | 小 | 将来を見据え、各戦いに次を追加：`type`(land/sea/air)・`startDate`/`endDate`・`theater`(戦域)・`sources[]`(出典)。**多言語・多国対応を見越し、表示文字列はキー化**（下記 Phase 3 参照）。 |
| **出典裏取りフロー** | 中 | `sources[{title,url}]` を持たせ、概要パネルに出典リンクを表示。仮データを順次一次資料で検証。 |

> Phase 0 は地味だが、以降のすべての機能（時間軸・一覧・多言語・多国）が乗る土台になる。**最初に投資すべき領域**。

### Phase 1 — 近期の機能拡張

| 優先 | 項目 | コスト | 内容 |
|------|------|--------|------|
| 高 | **時間軸スライダー / タイムライン** | 中 | 1941〜1945 の横スライダー。ドラッグでその時点までの戦いを点灯。再生ボタンで戦況推移を自動再生。**主題を最も強化する一手**。 |
| 高 | **戦い一覧 ＋ 絞り込み** | 中 | 時系列ソートのリスト。年・戦域・陸海・損害規模でフィルタ。俯瞰性とマーカー密集を解消。 |
| 中 | **URL による状態共有** | 小 | `location.hash` に戦い ID を保存し、起動時に復元。「この戦いを見て」と送れる。 |
| 中 | **OGP / SEO メタタグ整備** | 小 | `og:*` / `twitter:card` / `description` / `lang="ja"` / 構造化データ。地図サムネ PNG を同梱。 |

### Phase 2 — 体験の深化

| 優先 | 項目 | コスト | 内容 |
|------|------|--------|------|
| 中 | **進攻ルートの可視化** | 中〜大 | 動員元（郷土）→戦域（前線）を SVG の曲線矢印で結ぶ。2 図をまたぐ座標変換が要るが、主題の最も劇的な見せ方。 |
| 中 | **モバイル対応の強化** | 中 | 縦積みレイアウト＋タッチ操作（ピンチズーム）の最適化。 |
| 低 | **損害規模のマーカー反映** | 小 | `jpLoss` を数値化しマーカー径に反映。 |
| 低 | **導入ツアー / ナレーション** | 中 | 初訪問者向けに「1941 真珠湾 → 1945 沖縄」を辿る自動ツアー（タイムライン再生の延長）。 |

### Phase 3 — 長期ビジョン（プロジェクトの大目標）

#### 🌏 参戦国全体への拡大
太平洋戦争・日本中心から、**第二次世界大戦の参戦国全体**へ主題を一般化する。

- 「戦域 ↔ 動員地域」という構図を **各国に展開**：例）米国の州ごとの動員、英連邦（英・豪・加・印）、独・ソの各地域動員 など。
- データモデルを **国に依存しない形**へ：
  - 「本土地図（homeland map）」を国ごとに差し替え可能にする（現在の日本地図 = 一つの homeland）。
  - 各戦いに `belligerents`（参戦勢力）と、勢力ごとの `mobilization`（動員元の地域）を持たせる。
  - 視点（どの国を主役にするか）を切り替えられる UI。
- 戦域図は全世界戦域（欧州・北アフリカ・大西洋・東部戦線・太平洋）へ拡張。
- **アーキテクチャ上の含意**：Phase 0 のデータ拡張時点で「国・地域・勢力」を抽象化しておくと、後の作り直しを避けられる。

#### 🗣 多言語対応（i18n）
- UI 文言・戦いの解説文を **言語キー化**（例：`{ "ja": "...", "en": "..." }`）。
- 言語切替 UI、`lang` 属性と `hreflang` の整備。
- 地名・部隊名・固有名詞の対訳辞書。
- **設計上の注意**：和風の世界観は日本語前提で成立している面があるため、多言語版では「世界観の普遍化」とのバランスを設計する（フォント・タイポグラフィ含む）。

> ⚠️ 参戦国拡大・多言語化はいずれも **データモデルに深く関わる**。Phase 0 の段階で `type`・`belligerents`・`mobilization`・言語キーを織り込んでおくことが、後戻りを防ぐ鍵。

---

## 🎨 デザイン・見栄え（動的な刷新）

「静的な地図」から「動きのある戦史体験」へ。和風・古地図の世界観を保ったまま、動的な見え方を強化する。

### モーション・インタラクション
- **タイムライン再生時のアニメーション**：年が進むにつれマーカーが順に灯り、戦域が広がる/縮む様を見せる（Phase 1-A と一体で）。
- **進攻ルートの描画アニメーション**：矢印を一筆書きで引く（`stroke-dasharray` アニメ）。
- **選択時のトランジション強化**：日本地図の寄せ拡大に加え、概要パネルのフェード/スライドイン、関連都道府県の波及点灯。
- **マイクロインタラクション**：マーカーのホバー反応、境界線の明滅、ズーム時の慣性。

### 雰囲気・質感
- **和紙・古地図のテクスチャ**：紙の繊維、墨のにじみ、経年の汚し。背景に薄い地紋。
- **動的な背景演出**：ゆっくり流れる雲/波、光の差し込み（Ken Burns 風の微速ズーム）。やり過ぎは禁物、主題を邪魔しない範囲で。
- **時代/季節のライティング**：戦況の推移に合わせて全体トーンを変える（緒戦＝明、終盤＝沈鬱）案。
- **色覚多様性への冗長化**：陣営をハッチ模様（網代・麻の葉風）でも区別（色レビューの積み残し）。

### 演出の方針
- **「動的＝賑やか」ではなく「動的＝語る」**。アニメーションは情報（時間・進攻・因果）を伝えるために使い、装飾過多は避ける。
- パフォーマンス（SVG 要素数・再描画）とアクセシビリティ（`prefers-reduced-motion` 尊重）に配慮。

---

## 公開・運用

- **GitHub Pages** で公開（`main` から）。Phase 1 の URL 共有・OGP と合わせて。
- 公開ページに **「データは仮置き・要出典裏取り」** の注記を明示。
- 地図データ出典（Natural Earth / dataofjapan/land）のライセンス順守。

---

## 推奨着手順（検分レビュー）

1. **Phase 0：データ外部化＋構造拡張** — 全拡張の土台。多国・多言語を見越したモデルに。
2. **Phase 1：時間軸スライダー** — 主題を最も強化。サイトを「体験」へ昇格。
3. **Phase 1：URL 共有 ＋ OGP** — 極小コストで公開・拡散の前提を整える。

長期の大目標（参戦国拡大・多言語・動的デザイン）は、Phase 0 の設計判断に織り込みつつ、基盤が固まり次第、段階的に着手する。

---

*このロードマップは随時更新する。データはプロトタイプ用の仮データであり、一般公開前に一次資料での裏取りを行うこと。*
